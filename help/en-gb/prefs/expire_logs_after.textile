h1. Preferences: Logs expire after how many days?

When you are using Textpattern for logging, you can set how long (in days) those logs should be kept. '7' will keep the logs available for seven days.

p(alert-block information). *Note:* The logs only get cleared when you actually view the 'Visitor Logs' page, and will otherwise continue to build in size.
